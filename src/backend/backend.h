#ifndef VERB_backend_included
#define VERB_backend_included

// the backend's job is to:
//      lower code from bytecode to machine code (requires register naming).
//      optimise said machine code however it sees fit.

#endif 
