#ifndef VERB_preprocessor_ops_embed_included
#define VERB_preprocessor_ops_embed_included

// #embed works more or less the same as it does in C: it copies to its position a comma-separated list of bytes taken from some file.
// NOTE: #embed seems like too much work. add it l8r.

#endif
